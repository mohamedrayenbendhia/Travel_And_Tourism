{% extends 'base.html.twig' %}

{% block title %}New ReservationTransport{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="mb-4">Create new ReservationTransport</h1>
<div class="mb-3">
    {{ include('reservation_transport/_form.html.twig') }}
</div>
    <a href="{{ path('app_reservation_transport_index') }}" class="btn btn-secondary">back to list</a>
</div>
{% endblock %}
