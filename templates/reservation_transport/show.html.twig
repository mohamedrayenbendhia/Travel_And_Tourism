{% extends 'base.html.twig' %}

{% block title %}ReservationTransport{% endblock %}

{% block body %}
    <h1 class="mb-4">ReservationTransport</h1>
<div class="card p-4 shadow-lg rounded">

    <table class="table table-striped table-bordered">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ reservation_transport.id }}</td>
            </tr>
            <tr>
                <th>Date_reservation</th>
                <td>{{ reservation_transport.dateReservation ? reservation_transport.dateReservation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ reservation_transport.statut }}</td>
            </tr>
        </tbody>
    </table>
</div>

    <a href="{{ path('app_reservation_transport_index') }}" class="btn btn-secondary">back to list</a>

    <a href="{{ path('app_reservation_transport_edit', {'id': reservation_transport.id}) }}" class="btn btn-warning">edit</a>

    {{ include('reservation_transport/_delete_form.html.twig') }}
{% endblock %}
